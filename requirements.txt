# Installed by setup_env.sh into the `sam3` conda env, AFTER torch.
# These versions are the set already proven against facebook/sam3 on CHTC.
# torch itself is deliberately absent — see setup_env.sh for why (cu128).

transformers==5.8.1
accelerate==1.13.0
huggingface_hub==1.14.0
safetensors==0.7.0
tokenizers==0.22.2

pandas==3.0.3
numpy==2.4.4
pyarrow==24.0.0

pillow==12.2.0
requests==2.32.3
urllib3==2.3.0
tqdm==4.67.1

# build_index.py only — not needed on the GPU nodes, but harmless and it keeps
# one environment for the whole project.
duckdb==1.1.3
awscli==1.34.24
